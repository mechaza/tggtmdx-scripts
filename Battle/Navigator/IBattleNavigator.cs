using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Grawly.Battle.Navigator {

	/*/// <summary>
	/// Defines common functions of a BattleNavigator that I need to keep consistent between the BattleNavigator and the BattleNavigatorBehavior.
	/// It's just a way to make sure I keep those two classes daisy-chained together.
	/// It blows.
	/// </summary>
	public interface IBattleNavigator {

		#region PREPARATION
		/// <summary>
		/// Initializes the navigator with whatever it needs to get going.
		/// </summary>
		void Initialize();
		/// <summary>
		/// Preps the navigator for whatever context it needs when the specified battle is going to be run.
		/// </summary>
		/// <param name="battleTemplate">The BattleTemplate that this navigator will be commentating on.</param>
		void Prepare(BattleTemplate battleTemplate);
		#endregion

		#region NAVIGATOR LINES
		/// <summary>
		/// Requests a line for the BattleNavigator to say.
		/// </summary>
		/// <param name="requestType"></param>
		void RequestLine(BattleNavigatorRequestType requestType);
		#endregion

		#region INTERNAL COMPUTATION
		/// <summary>
		/// Generates a set of BattleNavigatorParams based on the request being made.
		/// </summary>
		/// <param name="requestType">The requested line type.</param>
		/// <returns></returns>
		BattleNavigatorParams GenerateBattleNavigatorParams(BattleNavigatorRequestType requestType);
		#endregion

	}*/


}