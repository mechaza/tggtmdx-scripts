using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Grawly {

	/// <summary>
	/// Just a simple enum that says what kind of screen should be loaded when the SaveCanvas is brought up.
	/// </summary>
	public enum SaveScreenType {
		Save = 0,		// The canvas should be one where the player can save.
		Load = 1,		// The canvas should be one where the player can load.
		
	}


}